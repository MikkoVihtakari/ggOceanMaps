# Convert line sizes measured as points to ggplot line sizes

Converts line sizes measured as points (as given by most programs such
as Adobe Illustrator etc.) to ggplot font sizes

## Usage

``` r
LS(x)
```

## Arguments

- x:

  numeric vector giving the lines sizes in points

## Value

Returns a numeric vector of lenght `x` of ggplot line sizes

## See also

Other size adjustors: [`FS()`](FS.md)
